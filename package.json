{
  "name": "rotaplanner",
  "version": "0.0.1",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild new_rotaplanner/ui/js.js new_rotaplanner/ui/css.css --sourcemap=linked  --bundle --outdir=new_rotaplanner/generated --metafile=meta.json "
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "esbuild": "^0.27.2"
  }
}
